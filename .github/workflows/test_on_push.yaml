name: CICD

on:
  push:
    branches:
      - testing

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout  
        uses: actions/checkout@v3

      - name: Deploy code
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          PROJECT_FOLDER: ${{ secrets.PROJECT_FOLDER }}
        run: |
          echo '--- SSH CONNECTION ---'
          # Создаем временный файл для ключа
          echo "$SSH_PRIVATE_KEY" > private_key
          chmod 600 private_key

          # Подключаемся к удаленному серверу
          ssh -i private_key -o StrictHostKeyChecking=no "$SSH_USERNAME@$SSH_HOST" << EOF
            echo '--- START WORK ON REMOTE SERVER ---'
            cd "$PROJECT_FOLDER" || exit
            echo '--- LIST OF FILES ---'
            ls -la
            echo '--- GIT ---'
            git checkout testing
            git pull
            cp .env-example .env
            echo '--- RESTARTING AND REBUILDING CONTAINER web ---'
            docker-compose up -d --build web
            echo '--- LIST OF DOCKER CONTAINERS AFTER RESTARTING web CONTAINER ---'
            docker ps -a
          EOF

          # Удаляем временный файл с ключом
          rm private_key

      # - name: Check SSH key length
      #   run: echo "${{ secrets.SSH_PRIVATE_KEY }}" | wc -c

      # - name: Set up SSH
      #   uses: webfactory/ssh-agent@v0.5.3
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USERNAME }}
      #     password: EKrQMRmUxFLCQ6zr1
      #     ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      # - name: List SSH keys
      #   run: ssh-add -l || echo "No keys added"

      # - name: Run command on remote server
      #   uses: appleboy/ssh-action@v0.1.4
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USER }}
      #     port: 22
      #     script: |
      #       echo '--- START WORK ON REMOTE SERVER ---'
      #       cd ${{ secrets.PROJECT_FOLDER }}
      #       echo '--- LIST OF FILES ---'
      #       git checkout testing
      #       git pull
      #       cp .env-example .env
      #       ls -a
      #       echo '--- RESTARTING AND REBUILDING CONTAINER web ---'
      #       docker-compose up -d --build web
      #       echo '--- LIST OF DOCKER CONTAINERS AFTER RESTARTING web CONTAINER ---'
      